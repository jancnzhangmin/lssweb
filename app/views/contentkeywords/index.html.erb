<div class="container bs-docs-container">
  <div class="panel panel-default">
    <!-- Default panel contents -->
    <div class="panel-heading">内容关键字</div>
    <div class="panel-body">

    </div>

    <!-- Table -->
    <table class="table">
      <th class="text-center">
        关键字
      </th>
      <th class="text-center">名称</th>
      <th class="text-center">操作</th>
      <%@contentkeywords.each do |contentkeyword| %>
          <tr>
            <td>
              <%= contentkeyword.name %>
            </td>

            <td>
              <%= contentkeyword.keyword %>
            </td>
            <td>
              <%= link_to '编辑',edit_contentkeyword_path(contentkeyword) %>
              <%= link_to '删除',contentkeyword, method: :delete, data: { confirm: '确认删除?' } %>
            </td>
          </tr>
      <% end %>
    </table>

  </div>
  <div class="text-right">
    <%= link_to "新增",new_contentkeyword_path,class: "btn btn-primary" %>
  </div>
</div>